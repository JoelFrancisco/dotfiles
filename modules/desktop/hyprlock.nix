{ ... }: {
  # hyprlock is installed in the hyprland module (+ PAM service).
  # User config is managed as a mutable file via hyprland's activation script.
  # This module exists for future hyprlock-specific system config if needed.
}
