{ ... }: {
  # hypridle is installed in the hyprland module.
  # User config is managed as a mutable file via hyprland's activation script.
  # This module exists for future hypridle-specific system config if needed.
}
