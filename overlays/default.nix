final: prev: {
  # Custom packages for AUR-only software that doesn't exist in nixpkgs.
  # Add derivations here as needed:
  #
  # aether = final.callPackage ./aether.nix {};
  # makima = final.callPackage ./makima.nix {};
  # wiremix = final.callPackage ./wiremix.nix {};
  # omarchy-walker = final.callPackage ./omarchy-walker.nix {};
}
